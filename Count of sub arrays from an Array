class UserMainCode:
    def countSubarrays(arr, n):
        count = 0
        for i in range(1, n-1):
            if arr[i-1] + arr[i+1] == arr[i]:
                count += 1
        return count
arr = [1, 2, 1, 3, 5, 2, 4, 2]
n = len(arr)
print(UserMainCode.countSubarrays(arr, n))   
